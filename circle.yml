machine:
  java:
    version: oraclejdk8
  timezone:
    Asia/Tokyo

dependencies:
  pre:
    - chmod +x ./gradlew

deployment:
  staging:
    branch: master
    heroku:
      appname: immense-meadow-12809
